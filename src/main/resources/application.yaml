spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    # 방금 만든 데이터베이스 이름을 끝에 붙여줍니다
    url: jdbc:mariadb://localhost:3306/service_platform
    username: root
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
